# Unsloth - optimized fine-tuning
unsloth

# Core ML dependencies
# Note: If you get "NVIDIA driver too old" errors, install torch separately first:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Or use setup.sh which auto-detects your CUDA version.
torch>=2.8.0
transformers==4.57.1
trl==0.26.2
datasets>=4.3.0
peft
accelerate
bitsandbytes

# Tokenizer support
sentencepiece
protobuf

# HuggingFace Hub
huggingface_hub>=0.34.0
hf_transfer

# Experiment tracking (optional)
# wandb
